"""Application layer — use cases and orchestration.

Depends on the domain layer and on ports (interfaces) only; infrastructure
adapters are injected at composition time (see ``leapconnect.container``).
"""
